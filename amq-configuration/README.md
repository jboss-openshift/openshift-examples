== AMQ custom configuration example ==

This is a very simple example of providing a custom configuration
file (a dummy 'hello world' file) that will be copied into the
image's `conf` directory when built via STI.
